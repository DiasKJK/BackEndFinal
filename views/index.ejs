<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
        <img src="https://mobimg.b-cdn.net/v3/fetch/e5/e594d337200536d04e08e6584a690275.jpeg" alt="Cooking blog"
            class="d-block mx-lg-auto img-fluid home-img" loading="lazy">
    </div>
    <div class="col-12 col-lg-6">
        <h1 class="display-5 fw-bold mb-3">Wide varities of delicious recipe ideas</h1>
        <p>
            Expore our huge selection of delicious recipe ideas including easy desserts,vee and non-veg dinner ideas,
            yummy pasta recipes, quick bakes, family friendly meals and gluttten-free recipes
        </p>
        <div class="d-grid d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>
            <a href="/explore-random" class="btn btn-outline-secondary btn-light btn-lg px-4 me-md-2">Show Random</a>
        </div>
    </div>
</div>

<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">

    <% if(categories !='' ) { %>
        <% categories.forEach(function(category, index) { %>
            <a href="/categories/<%- category.name %>" class="col text-center category_link">
                <div class="category_img shadow">
                    <img src="img/<%- category.image %>" alt="<%- category.name %>" loading="lazy">
                </div>
                <div class="pt-1">
                    <h5>
                        <%- category.name %>
                    </h5>

                    <!-- <% if(userName){ %>
                        <%- userName %>
                    <% } %> -->
                </div>
            </a>
            <% }) %>
                <% } %>

                    <a href="/categories" class="col text-center text-decoration-none">
                        <br>
                        <div class="pt-1">View All</div>
                    </a>
</div>

<!-- latest start -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(food !='' ) { %>
            <% food.latest.forEach(function(recipe, index){ %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category_link">
                    <div class="category_img--large shadow">
                        <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <h5>
                            <%= recipe.name %>
                        </h5>
                    </div>
                </a>
                <% }) %>
                    <% } else { %>
                        <p>No items found.</p>
                        <% } %>
    </div>
</section>
<!-- latest end -->
<section class="main__wrapper">
    <link href="./index.css" rel="stylesheet" type="text/css">
    <!-- 👇 All carousels render here -->
    <div class="carousel__items" id="main__slider">

        <!-- carousel arrows ⬅ ➡ -->
        <a class="arrows prev" role="button" onclick="nextSlide(-1)">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        </a>
        <a class="arrows next" role="button" onclick="nextSlide(1)">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
        </a>

        <!-- carousel indicators -->
        <ul class="carousel-dots"></ul>
    </div>

    <!-- carousel controllers 🎮 -->
    <div class="carousel__controllers">

        <div class="carousel-update-container">

            <!-- Add new carousel -->
            <div class="add-carousel-container">
                <h1 class="controllers-header">Add New Carousel</h3>
                    <form onsubmit="event.preventDefault(); addCarousel();" class="main__form"
                        id="add_carousel_form" spellcheck="false">
                        <div class="form-group">
                            <label for="carousel_label_input">Enter carousel text (optional)</label>
                            <input type="text" class="form-control" id="carousel_label_input" />
                        </div>

                        <div class="form-group">
                            <input type='file' onchange="readURL(this);" id="add__carousel__input" />
                            <div class="thumbnail-container">
                                <img id="preview_image" src="./assets/thumbnail.jpg" alt="thumbnail"
                                    accept="image/*" />
                                <img src="./assets/cancel.svg" alt="close" class="cancel__svg"
                                    onclick="onCancelPreview()" />
                            </div>
                        </div>

                        <label for="available-slides__add">Choose position to add:</label>
                        <select id="available-slides__add" class="selectIndex" onfocus="populateDropdown()">
                        </select>
                        <select class="selectIndex" id="add__position">
                            <option>After</option>
                            <option>Before</option>
                        </select>

                        <% if (userName) { %>
                            <button type="submit" id="add-carousel" class="btn">Add Carousel</button>
                        <% } else { %>
                            <p>You need to be logged in to add a carousel.</p>
                        <% } %>
                    </form>
            </div>

            <!-- carousel toggles -->
            <div class="carousel-toggle-controls">
                <h1 class="controllers-header">Controllers</h1>
                <form class="main__form">

                    <!-- Arrows Toggler -->
                    <div class="controller arrow__toggle__controller">
                        <input type="checkbox" id="toggle_arrows" onclick="toggleArrows(this)" checked />
                        <label for="toggle_arrows">Toggle Arrows</label>
                    </div>

                    <!-- Bottom indicator toggler -->
                    <div class="controller indecator__controller">
                        <input type="checkbox" id="toggle_indicator" onclick="toggleDots(this)" checked />
                        <label for="toggle_indicator">Toggle Indicator</label>
                    </div>

                    <!-- Number indicator toggler -->
                    <div class="controller number_indecator__controller">
                        <input type="checkbox" id="toggle_number_indicator" onclick="toggleNumberIndicator(this)" />
                        <label for="toggle_number_indicator">Toggle to Number Indicators</label>
                    </div>

                    <!-- Auto switch carousel -->
                    <div class="controller carousel__autoswitch__controller">
                        <input type="checkbox" id="toggle_autocarousel_indicator"
                            onclick="toggleAutoCarousel(this)" />
                        <label for="toggle_autocarousel_indicator">Toggle to Auto slide</label>
                    </div>
                </form>
            </div>

        </div>

    </div>

</section>

<script type="text/javascript" src="/index.js" defer></script>

<!-- latest start -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Thai Recipes</h2>
        <a href="/categories/Thai" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.thai !='undefined' && food.thai.length> 0) { %>
            <% food.thai.forEach(function(recipe, index){ %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category_link">
                    <div class="category_img--large shadow">
                        <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <h5>
                            <%= recipe.name %>
                        </h5>
                    </div>
                </a>
                <% }) %>
                    <% } else { %>
                        <p>No items found.</p>
                        <% } %>
    </div>
</section>
<!-- latest end -->

<!-- latest start -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Indian Recipes</h2>
        <a href="/categories/Indian" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.indian !='undefined' && food.indian.length> 0) { %>
            <% food.indian.forEach(function(recipe, index){ %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category_link">
                    <div class="category_img--large shadow">
                        <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <h5>
                            <%= recipe.name %>
                        </h5>
                    </div>
                </a>
                <% }) %>
                    <% } else { %>
                        <p>No items found.</p>
                        <% } %>
    </div>
</section>
<!-- latest end -->

<!-- latest start -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Chineese Recipes</h2>
        <a href="/categories/Chineese" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(typeof food.chineese !='undefined' && food.chineese.length> 0) { %>
            <% food.chineese.forEach(function(recipe, index){ %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category_link">
                    <div class="category_img--large shadow">
                        <img src="/uploads/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <h5>
                            <%= recipe.name %>
                        </h5>
                    </div>
                </a>
                <% }) %>
                    <% } else { %>
                        <p>No items found.</p>
                        <% } %>
    </div>
</section>
<!-- latest end -->

<!-- submit start -->
<section class="px-4 py-5 my-5 text-center">
    <img src="/img/publish-recipe.png" alt="Publish your recipe for free today" class="d-block mx-auto mb-4 img-fluid"
        loading="lazy" width="566" height="208">
    <h1 class="display-5 fw-bold">Publish your recipe for FREE today</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">Publish your recipe in front of thousands of people for free</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="/submit-recipe" class="btn btn-primary btn-dark btn-lg">Submit Recipe</a>
        </div>
    </div>
</section>
<!-- submit-end -->
